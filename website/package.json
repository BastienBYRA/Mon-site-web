{
  "name": "website",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "old_start": "npx @11ty/eleventy --serve --watch --port=8081",
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve --port=8081",
    "sass": "npx sass src/styles/:_site/styles/",
    "watch:sass": "npx sass src/styles/:_site/styles/ --watch",
    "start": "npm run serve & npm run watch:sass"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0"
  },
  "dependencies": {
    "@sindresorhus/slugify": "^2.2.1",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^8.6.7",
    "markdown-it-attrs": "^4.1.6",
    "sass": "^1.76.0",
    "slugify": "^1.6.6"
  }
}
